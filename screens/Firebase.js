import { View, Text, ScrollView, StyleSheet } from "react-native";

const FirebaseApp = () => {
  return (
<View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
    <Text>Firebase App Screen</Text>
  </View>
  );
};

export default FirebaseApp;

