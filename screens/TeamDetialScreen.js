import { View, Text, ScrollView, StyleSheet } from "react-native";

const TeamDetialScreen = () => (
  <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
    <Text>Team Details Screen</Text>
  </View>
);

export default TeamDetialScreen;
