import { View, Text, ScrollView, StyleSheet } from "react-native";

const DraftComponent = () => (
  <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
    <Text>Draft Component</Text>
  </View>
);

export default DraftComponent;
